import React from 'react'
import style from './expression-view.module.scss'

/** Shows textual representation of a provided expression. */
export const ExpressionView = ({ expression }) => <div className={style.container}>
  ExpressionView
</div>
